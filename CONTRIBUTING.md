# Contributing

感谢你对本项目的关注。

本仓库用于维护一套面向 Claude Code 的仓库级规则系统。  
在提交改动前，请先理解本仓库的核心原则：

- 最小优先
- 按需加载
- 够用即停
- 避免 prompt 膨胀
- 优先稳定性，而不是规则数量

---

## 贡献前建议

在提交 PR 之前，请先自问：

1. 这条规则是否必须进入常驻主文件？
2. 能否放到一个更小的外部规约文件里？
3. 它是在减少重复，还是引入更多重复？
4. 它会提升长期稳定性吗？
5. 它是否会增加上下文膨胀风险？

如果这些问题里有多个答案偏负面，建议先不要把变更并入主文件。

---

## 贡献方向

欢迎以下类型的贡献：

- 修正文档错误、链接错误、命名错误
- 优化 `CLAUDE.md` 的表达，使其更稳定、简洁、可执行
- 增加新的极简规约文件
- 增加新的输出模板
- 提出更清晰的分层方式
- 改进 examples 示例
- 改进项目结构与可维护性

---

## 不建议的贡献方向

以下改动通常不建议直接提交：

- 把 `CLAUDE.md` 扩展成巨大的万能提示词
- 在主文件中加入大量低频、场景化细节
- 为了“完整”而堆砌规则
- 引入重复表达或大段说明性文字
- 在没有明确收益的情况下增加复杂度

---

## 文件修改建议

### 修改 `CLAUDE.md` 前
请优先判断：

- 这是否属于高频、稳定、高杠杆规则？
- 这是否应该放到 `.claude/rules/task/`？
- 这是否应该放到 `.claude/rules/templates/`？
- 这是否应该放到 `.claude/rules/preferences/`？

主文件应尽量保持短、小、稳。

### 新增规则文件时
请尽量遵循：

- 文件职责单一
- 规则数量少
- 用语清晰
- 不依赖长背景
- 可以独立被引用
- 命名保持 `*-min.md`

---

## 提交风格建议

建议提交信息清晰说明改动意图，例如：

- `docs: refine README wording`
- `rules: add sub-agent preference minimal rule`
- `templates: add multi-agent summary template`
- `refactor: reduce duplication in CLAUDE.md`

---

## Pull Request 建议

PR 描述建议至少包含：

1. 改动目标
2. 为什么要改
3. 改动范围
4. 是否增加了上下文或维护成本
5. 为什么该改动应该放在当前层，而不是其他层

---

## 讨论建议

如果你不确定某条规则是否应该加入，建议先开 issue 或 draft PR 讨论，而不是直接扩大主文件。

---

## 行为约定

请保持讨论：

- 简洁
- 聚焦问题
- 基于长期可维护性
- 尊重已有设计边界

感谢贡献。